import { useState } from 'react'
import type { CartItem } from './types'

function App() {
  const [cart, setCart] = useState<CartItem[]>([])

  

  return (
    <>
      <p>a</p>
    </>
  )
}

export default App
